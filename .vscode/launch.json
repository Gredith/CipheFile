{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Compilar y ejecutar",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/programa.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "preLaunchTask": "build"
        }
    ]
}
